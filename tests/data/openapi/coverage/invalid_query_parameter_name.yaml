openapi: 3.0.0
info:
  title: Invalid parameter
  version: 1.0.0
paths:
  /pets:
    get:
      operationId: listPets
      parameters:
        - name: rs:query
          in: query
          description: How many items to return at one time
          required: false
          schema:
            type: integer
            format: int32
      responses:
        '200':
          description: OK
